<UserControl x:Class="TestPassModule.Ui.Edit.PropertyEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestPassModule.Ui.Edit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <UserControl.Resources>
        <ResourceDictionary Source="/Ui/dic/dic-properties-editor.xaml" />
    </UserControl.Resources>
    <StackPanel>
        <Label Content="Position:" x:Name="txtTitle"
               Style="{StaticResource input-label-title}" />

        <Grid Margin="10,0,0,0" x:Name="ContentGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
       
        </Grid>

    </StackPanel>
</UserControl>
